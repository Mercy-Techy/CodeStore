function run(){
    let student = ["bolu", "samson", "mercy", "emmanuel"];
    // map.map is es 6 version 4 for loop
    let number = [2,5,7,8,3]
    number.filter(i=>console.log(i))
    // for(let i=0;i<number.length;i++){
    //     console.log(number[i]*5);
    // }
    // it is also equivalent to this
    // number.map(i=>console.log(i*5))
    // number.filter(i=>console.log(i<=7))
    // number.filter(i=>i<=7?console.log(i):"")

    // number.filter(i=>(i*(3))%2 != 0?console.log(i*(3/2)):"")
    // let name = document.getElementById("input").value;
    // let text
    // student.filter(i => name.toLowerCase() == i ? text = i[0].toUpperCase() + i.slice(1) : "");
    // document.getElementById("last").innerHTML = text
}
// push to a new array using for loop to check each item use the same loop
run()